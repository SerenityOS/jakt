/// Expect:
/// - error: "No function with matching signature found."

class A {
    pub fn foo(this) => 0
}

class B : A {
    pub fn foo(this, x: String) => 0
}

fn main() {
    let b = B()
    b.foo()
}
