/// Expect:
/// - error: "Cannot create an array with values of type void\n"

enum Bar {
    Value
}

function main() {
    let foo = Bar::Value()
    [match foo {
        else => {
        }
    }]
}
