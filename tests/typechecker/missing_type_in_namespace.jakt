/// Expect:
/// - error: "Unknown type"

extern struct StringBuilder {
    fn append(mut this, anon s: u8)
    fn to_string(mut this) throws -> String
    fn create() throws -> StringBuilder
}

namespace lexer {
struct Lexer {
}
}

fn main(args: [String]) {
    mut tokens: [Token] = []
}
