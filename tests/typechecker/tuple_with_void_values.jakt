/// Expect:
/// - error: "Cannot create a tuple that contains a value of type void\n"

enum Bar {
    Value
}

fn main() {
    let foo = Bar::Value()
    let b = (match foo {
        else => {
        }
    }, 1)
}
