/// Expect:
/// - output: "1\n"
enum Foo {
    Bar
    Baz
}

fn test(foo: Foo) -> i64 {
    match foo {
        Bar => { return 1 }
        Baz => { return 2 }
    }
}

fn main() {
    let foo = Foo::Bar
    println("{}", test(foo))
}
