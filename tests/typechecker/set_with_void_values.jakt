/// Expect:
/// - error: "Cannot create a set with values of type void\n"

enum Bar {
    Value
}

fn main() {
    let foo = Bar::Value()
    let b = {match foo {
        else => {
        }
    }}
}
