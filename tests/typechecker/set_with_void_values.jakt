/// Expect:
/// - error: "cannot create a set with values of type void\n"

enum Bar {
    Value
}

function main() {
    let foo = Bar::Value()
    let b = {match foo {
        else => {
        }
    }}
}
