/// Expect:
/// - output: "success\n"

fn main() throws {
    if true {
        let x = [1, 2]
    } else {
        let x = [1]
    }

    // should parse without crashing the compiler
    println("success")
}
