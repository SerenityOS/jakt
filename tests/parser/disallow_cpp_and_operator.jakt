/// Expect:
/// - error: "‘&&’ is not allowed, use ‘and’ instead"
fn main() {
    let x = "hello"
    if x.length() > 6 && x.substring(start: 0, length: 6) == "world!" {
        println("hello, {}", x)
    }
}
