struct Rectangle {
    width: i64,
    height: i64,

    fun grow(mut this) {
        this.width *= 2;
        this.height *= 2;
    }

    fun area(this) => this.width * this.height
}

fun main() {
    let mut rect = Rectangle(width: 4, height: 8)

    rect.grow()

    println("{}", rect.area())
}