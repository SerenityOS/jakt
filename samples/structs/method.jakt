struct Rectangle {
    width: i64,
    height: i64,

    function area(this) => this.width * this.height
}

function main() {
    let rect = Rectangle(width: 4, height: 8)

    println("{}", rect.area())
}