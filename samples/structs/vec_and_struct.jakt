/// Expect:
/// - output: "Jane\nBjörn\n"

struct Person {
    name: String
    age: i64
}

fn main() {
    let arr = [
        Person(name: "Jane", age: 100), 
        Person(name: "Björn", age: 200)
    ]

    println("{}", arr[0].name)
    println("{}", arr[1].name)
}
