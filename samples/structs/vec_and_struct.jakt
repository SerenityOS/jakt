struct Person {
    name: String
    age: i64
}

function main() {
    let arr = [
        Person(name: "Jane", age: 100), 
        Person(name: "Björn", age: 200)
    ]

    println("{}", arr[0].name)
    println("{}", arr[1].name)
}