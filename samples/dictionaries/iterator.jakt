/// Expect:
/// - output: "well: 1\nhello: 2\nfriends: 3\n"

fn main() {
    let dictionary = ["well": 1, "hello": 2, "friends": 3]
    for item in dictionary {
        println("{}: {}", item.0, item.1)
    }
}
