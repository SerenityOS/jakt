class Foo {
    x: i64

    fun set(mut this, value: i64) {
        this.x = value
    }

    fun get(this) {
        return this.x
    }
}

fun main() {
    let mut foo = Foo(x: 1)
    let mut opt_foo: Foo? = foo

    opt_foo!.set(value: 2)

    println("{}", opt_foo!.get())
}
