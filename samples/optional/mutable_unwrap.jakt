/// Expect:
/// - output: "2\n"

class Foo {
    pub x: i64

    pub fn set(mut this, value: i64) {
        this.x = value
    }

    pub fn get(this) => this.x
}

fn main() {
    mut foo = Foo(x: 1)
    mut opt_foo: Foo? = foo

    opt_foo!.set(value: 2)

    println("{}", opt_foo!.get())
}
