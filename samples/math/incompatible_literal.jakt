/// Expect:
/// - error: "Binary arithmetic operation between incompatible types (‘u8’ and ‘u16’)\n"

fn main() {
    let x: u8 = 12;
    let y: u16 = 34;
    println("{}", x + y);
    println("{}", x - y);
    println("{}", x * y);
    println("{}", x / y);
}
