/// Expect:
/// - output: "3 bar\n"

fn main() {
    let x = match 42 {
        (21 * 2) => [1, 2, 3]
        else => []
    }
    let y = match "foo" {
        ("foo") => "bar"
        else => "baz"
    }

    println("{} {}", x.size(), y)
}
