extern struct StringBuilder {
    fun append(var this, anon s: raw char) {}
    fun to_string(var this) -> String {}
}

fun main() {
    var s = StringBuilder()

    s.append("abc".characters());
    s.append("123".characters());

    print(s.to_string())
}