/// Expect:
/// - output: "Jane\n100\n"

class Person {
    pub name: String
    pub age: i64
}

fn main() {
    let p = Person(name: "Jane", age: 100)

    println("{}", p.name)
    println("{}", p.age)
}
