class Person {
    name: String
    age: i64
}

function main() {
    try {
        let p = Person(name: "Jane", age: 100)

        println("{}", p.name)
        println("{}", p.age)
    } catch err {}
}