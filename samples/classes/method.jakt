/// Expect:
/// - output: "1001\n"

class Person {
    pub name: String
    pub age: i64

    pub fn birthday(mut this) {
        ++this.age
    }
}

fn main() {
    mut p = Person(name: "Bob", age: 1000)

    p.birthday()

    println("{}", p.age)
}
