class Person {
    name: String
    age: i64

    function birthday(mut this) {
        ++this.age
    }
}

function main() {
    let mut p = Person(name: "Bob", age: 1000)

    p.birthday()

    println("{}", p.age)
}