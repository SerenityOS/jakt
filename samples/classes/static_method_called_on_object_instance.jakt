/// Expect:
/// - error: "Cannot call static method on an instance of an object"

class Foo {
    public function bar() {
        println("I am a static method")
    }
}

function main() {
    let foo = Foo()
    foo.bar()
}