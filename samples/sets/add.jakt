function main() {
    try {
        let mutable set = {"b", "c"}
        set.add("a")
        println("{}", set.size())

        set.add("b")
        set.add("c")
        println("{}", set.size())
    } catch err {}
}
