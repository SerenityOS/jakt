/// Expect:
/// - output: "2 2\n"

function main() {
    let mutable set1: [i64] = []
    set1.push(1)
    set1.push(2)
    let mutable set2: [[[String]]] = []
    set2.push([["hello"]])
    set2.push([["world"]])
    println("{} {}", set1.size(), set2.size())
}
